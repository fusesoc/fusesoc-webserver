"""App configuration for the core_directory app."""

from django.apps import AppConfig

class CoreDirectoryConfig(AppConfig):
    """Configuration class for the core_directory app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'core_directory'
